import express from 'express';
import Property from '../models/Property.js';  // Ensure this path is correct
import User from '../models/User.js'; // Similarly for User model

const router = express.Router();

// Define your routes here

export default router;
